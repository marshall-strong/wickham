<%= form_with(model: paper_product) do |form| %>
  <% if paper_product.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(paper_product.errors.count, "error") %> prohibited this paper_product from being saved:</h2>

      <ul>
        <% paper_product.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
