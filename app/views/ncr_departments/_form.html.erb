<%= form_with(model: ncr_department) do |form| %>
  <% if ncr_department.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(ncr_department.errors.count, "error") %> prohibited this ncr_department from being saved:</h2>

      <ul>
        <% ncr_department.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
