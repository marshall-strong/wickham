<%= form_with(model: food_product) do |form| %>
  <% if food_product.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(food_product.errors.count, "error") %> prohibited this food_product from being saved:</h2>

      <ul>
        <% food_product.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
